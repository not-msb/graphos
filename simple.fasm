format ELF64 executable
org 0x7c00

segment readable executable
entry main
main:
    mov rax, 1
    mov rdi, 1
    mov rsi, msg
    mov rdx, msg_len
    syscall

    mov rax, 60
    mov rdi, 0
    syscall

segment readable writeable
    msg db "Hello, World!", 10
    msg_len = $ - msg