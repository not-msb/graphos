org 0x7c00

i = 0
repeat msg_len
    mov al, [msg+i]
    int 0x10
    i = i + 1
end repeat

jmp enter_pm
jmp $

use32
kmain:
    jmp $

msg db "Still prints :)"
msg_len = $ - msg

include "gdt.fasm"
include "switch.fasm"

repeat 510-($-$$)
    db 0
end repeat
dw 0xaa55